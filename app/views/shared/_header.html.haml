%header.header
  .header__bar.row
    .header__title
      %h1.grid-6
        = link_to "Chanimal", root_path, class: "header-chanimal"
      - if user_signed_in?
        .user_nav.grid-6
          %span
            = current_user.nickname
            %ul.user__info
              %li
                = link_to "マイページ", "/users/#{current_user.id}"
                = link_to "ログアウト", destroy_user_session_path, method: :delete

      - else
        .grid-6
          = link_to "ログイン", new_user_session_path, class: 'post'
          = link_to "新規登録", new_user_registration_path, class: 'post'

      .user_nav.grid-6
        %a.btn.header-post(href= new_tweet_path)
          %span.hovering 投稿する！
          %span.default　投稿する？